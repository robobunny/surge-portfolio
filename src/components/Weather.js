import React from 'react';
import WeatherIcon from 'react-icons-weather';

const Weather = () => (
  <div className="weather">
    <WeatherIcon name="owm" id="800" />
  </div>
)

export default Weather;